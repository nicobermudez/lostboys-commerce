import React from "react"
import styled from "@emotion/styled"

const FooterContainer = styled.footer({
    position: "absolute",
    bottom: "0",
})

const Footer = () => {
    return (
        <FooterContainer>© Lost Boys Creations</FooterContainer>
    )
}

export default Footer
